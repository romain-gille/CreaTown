<div class="container sticky_footer py-3 ">
  <%= render "partials/dashboard-menu" %>
  <div>
    <h1>Liked creations</h1>
    <% if @creations_liked.empty? %>
      <h2 class="text-center my-3">You have not liked any creations yet</h2>
    <% else %>
      <div class="card-container-dashboard">
        <% @creations_liked.each do |creation| %>
          <%= render :partial => 'partials/creation-card', locals: { creation: creation, user: @user } %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
