<%= simple_form_for(@creation) do |f| %>
  <%= f.input :name %>
  <%= f.input :category %>
  <%= f.input :description %>
  <%= f.input :photo, as: :file %>
  <%= f.submit class: "btn btn-custom" %>
<% end %>
